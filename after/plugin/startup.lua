-- ~/.config/nvim/lua/startup.lua

-- Load the startup.nvim plugin
require('startup')

-- Configure the Evil theme
require('startup').setup({
    theme = 'evil', -- Set the theme to 'evil'
    -- Add other configuration options as needed
})

